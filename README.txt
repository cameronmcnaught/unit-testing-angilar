Make sure you have Karma installed globally on your machine.

To run the test case:
 1) Open a terminal
 2) Navigate to the project root folder (Testing)
 3) Run the command (without quotes): 'karma start app/karma.conf.js'

To view the coverage of the app:
 1) Open the index.html file found in <path to root folder>/app/coverage/index.html